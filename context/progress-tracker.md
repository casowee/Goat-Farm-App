# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 2 — Core records: in progress. Family tree (06) is complete (parents/pedigree, multi-breed composition, parent-based breed computation, barn-move history). Next is health records (07). Goat photo upload (from 05) remains a deferred follow-up, not phase-blocking.

## Current Goal

- **Health records (`context/feature-specs/07-health-records.md`, ◀ next)** — health history, vaccinations, deworming, and medicine records, each dated with next-due dates where relevant. Note the legacy pre-spec tables `vaccinations` / `medicine_records` collide with names 07 plans to create fresh (see Open Questions) — resolve that first. The deferred goat photo-upload follow-up (from 05) is also still open.

## Completed

- Context/spec pack written and aligned: `project-overview.md`, `architecture-context.md`, `ui-context.md`, `code-standards.md`, `ai-workflow-rules.md`, and `AGENTS.md`.
- Next.js + TypeScript + Tailwind project scaffolded (pre-existing before this session).
- **Design system** (`context/feature-specs/01-design-system.md`): desert palette tokens added to `app/globals.css` on `:root` and mapped to Tailwind utilities via `@theme inline`; shadcn/ui theme variables (`--background`, `--card`, `--primary`, etc.) pointed at the desert palette; `--radius` wired to the `rounded-xl` / `2xl` / `3xl` scale. Geist Sans and Geist Mono loaded via `next/font/google` in `app/layout.tsx`. shadcn/ui initialized (this project's shadcn version uses `@base-ui/react` primitives and a `render`-prop composition API rather than Radix's `asChild`); added Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea to `components/ui/`. `lib/utils.ts` exports a working `cn()`. `npm run build` passes; `/style-check` verified in a real browser (Playwright/Chromium) with no console errors — near-black background, sand/terracotta accents, warm text, dialog backdrop blur, no light-mode flash.
- **Auth and login** (`context/feature-specs/02-auth-and-login.md`): `@supabase/ssr` + `@supabase/supabase-js` installed. `lib/supabase/client.ts` (browser client), `lib/supabase/server.ts` (server client wired to `cookies()`), `lib/supabase/middleware.ts` (`updateSession` helper) added. Route protection runs in `proxy.ts` at the project root — this Next.js version (16.3.2) deprecated `middleware.ts` in favor of `proxy.ts` (function renamed `middleware` → `proxy`; same `NextRequest`/`NextResponse`/`matcher` API otherwise), confirmed by the user rather than guessed. `app/login/actions.ts` has the `login()` Server Action; `lib/actions/auth.ts` has `signOut()`. `app/login/page.tsx` is a client component using `useActionState` + `useFormStatus` for pending/error states, built on the design-system `Card`/`Input`/`Button`. All previously-existing app routes moved under `app/(app)/` behind a server-side auth check in `app/(app)/layout.tsx` (defense in depth alongside the proxy). `npm run build` passes. Verified in a real browser (Playwright/Chromium) against a fake Supabase URL: signed-out `/` redirects to `/login` (307), `/login` renders the dark-desert theme with no light-mode flash (`main` background resolves to `#0B0A09`), and a failed sign-in shows the inline error without crashing or throwing console errors. The owner has since filled in real Supabase project credentials in `.env.local`.
- **App shell** (`context/feature-specs/03-app-shell.md`): shadcn's `sidebar` block added (`npx shadcn@latest add sidebar`), which pulled in `Separator`, `Skeleton`, `Tooltip`, `Sheet`, and `hooks/use-mobile.ts` — none of `components/ui/*` were hand-edited. The sidebar's desert-palette tokens (`--sidebar`, `--sidebar-foreground`, etc.) were already wired in `app/globals.css` back in the design-system step, so no CSS changes were needed here. Nav config lives in one place, `lib/nav.ts` (an array of `{ label, href, icon }`; `code-standards.md` has no `constants/` convention, so `lib/` was used per the spec's fallback). `components/app-sidebar.tsx` (client component: maps `lib/nav.ts`, highlights the active route via `usePathname()`, sign-out button in the footer calling the step-02 `signOut()` action) and `components/top-bar.tsx` (holds `SidebarTrigger` + current page title, sticky) replace the ad-hoc header that had lived in `app/(app)/layout.tsx`; that layout now wraps `{children}` in `SidebarProvider` and still keeps the server-side session guard from step 02. `components/sign-out-button.tsx` (the standalone version from step 02) was deleted as dead code once sign-out moved into the sidebar footer. Stub pages added for all 9 routes (`/`, `/goats`, `/medicine`, `/health`, `/breeding`, `/weight`, `/vaccinations`, `/deworming`, `/sales`) via a shared `components/module-placeholder.tsx`. `npm run build` passes. Verified in a real browser (Playwright/Chromium) at both desktop and iPhone (390×844) widths: active nav item highlights correctly, desktop sidebar persists, mobile hamburger opens a drawer that closes on both link-tap and backdrop-tap, sign-out button reachable in the sidebar footer, dark-desert theme throughout, `/login` still unwrapped. One real bug caught and fixed during this verification: the mobile drawer stayed open after a nav-link tap because `SidebarProvider` lives in the persisting layout (route changes don't remount it), so `AppSidebar` now calls `setOpenMobile(false)` on link click when `isMobile`.
- **Barns** (`context/feature-specs/04-barns.md`) — the first real CRUD module, establishing the pattern later modules copy. `types/database.types.ts` is now the real generated output from `npm run gen:types` (script uses `npx supabase gen types typescript --project-id tfzzvtizokakcmwmvmhu --schema public > types/database.types.ts`; switched from a bare `supabase` call to `npx supabase` since the CLI isn't installed globally in this environment) — the owner supplied `SUPABASE_ACCESS_TOKEN` via `.env.local` after this module was first built (it was initially hand-written against the task-1 SQL as a stand-in, then confirmed to match exactly once real generation ran). The token was read out of `.env.local` and passed to the one `npm run gen:types` invocation entirely inside a PowerShell variable, never echoed, printed, or included in any command text. The generated file also revealed the owner had already run the `barns` table + RLS SQL themselves, and covers all 9 existing tables, not just barns. Both Supabase clients (`lib/supabase/client.ts`, `lib/supabase/server.ts`) are generic over `Database`, so every query is type-checked — confirmed by temporarily selecting a nonexistent column and watching `tsc` fail with a `SelectQueryError`, then reverting. `Barns` added to the existing `lib/nav.ts` (no second nav list). `app/(app)/barns/page.tsx` (Server Component list, RLS-scoped, empty state), `app/(app)/barns/actions.ts` (`createBarn`/`updateBarn`/`deleteBarn`, each `revalidatePath('/barns')`, insert never sets `owner_id` since the column default handles it), `components/barns/barn-form-dialog.tsx` (one dialog reused for add/edit via an optional `barn` prop, `useActionState` + `useFormStatus`), `components/barns/delete-barn-dialog.tsx` (confirmation dialog). `npm run build` passes. Verified in a real browser (Playwright/Chromium, same temporary-auth-bypass-then-revert technique as the app-shell step) both before and after the real table was confirmed to exist: `/barns` renders dark-desert with the "Barns" nav item highlighted, the empty state (0 rows, RLS-filtered) shows correctly with no crash, the Add Barn dialog opens, and a whitespace-only name is correctly rejected with an inline "Name is required." error. Full authenticated CRUD (add/edit/delete as the owner, RLS isolation across two accounts) still needs the owner's own logged-in test in the browser, since testing that requires their real credentials.
- **Goat profiles core** (`context/feature-specs/05-goat-profiles.md`) — the second CRUD module, copying the barns pattern and extending it with a barn foreign key, derived stage logic, and (deferred) file upload. `goats` table did not previously exist (the pre-existing `goat_records` table is unrelated legacy data, see the open question below), so it was created fresh via `supabase/migrations/20260826000001_goats.sql`, run by the owner in the Supabase SQL editor. Reconciled against the real `barns` table (which uses `bigserial` integer ids, not `uuid`, and a single `"owner full access" for all` RLS policy) rather than the spec's literal `uuid` sample, per the spec's own instruction to match whatever convention the prior migration established: `goats.id`/`barn_id`/`sire_id`/`dam_id` are all `bigint`, and RLS is one `"owner full access"` policy. Per the owner, `tag` is the required identifier and `name` is optional (the reverse of the spec's literal default). `lib/goats/stage.ts` has the pure `deriveGoatStage`/`ageInMonths` functions with `STAGE_THRESHOLDS_MONTHS` confirmed at `kidMax: 6`, `youngStockMax: 12` (general-purpose values, not breed-specific — see Architecture Decisions). `app/(app)/goats/actions.ts` has `createGoat`/`updateGoat`/`deleteGoat`, validating tag/sex/date-of-birth/status/barn server-side and forcing `reproductive_state: 'intact'` for females regardless of form input. Added shadcn `select`, `badge`, and `table` components via the CLI (none hand-rolled). `app/(app)/goats/page.tsx` (list, barn filter via the `searchParams` page prop passed down to the client `BarnFilter`, table on desktop / stacked cards on phone), `app/(app)/goats/[id]/page.tsx` (detail page with a stage badge, core-details grid, and `Tabs` placeholder sections for Health/Weight/Breeding/Lineage — all deferred to 06–09), `components/goats/goat-form-dialog.tsx` (add/edit dialog; the reproductive-state field only renders when Sex = Male, matching the spec), `components/goats/delete-goat-dialog.tsx`, `components/goats/goat-stage-badge.tsx`, `components/goats/barn-filter.tsx`. **Photo upload (task 11 of the spec) was deferred to its own follow-up increment per the owner** — the spec explicitly allows this split since goats are fully usable without a photo. `npm run build` passes; the generated-types wiring was re-confirmed by temporarily selecting a nonexistent column and watching it fail, then reverting. Verified in a real browser (Playwright + a temporary `proxy.ts`/`app/(app)/layout.tsx` auth-bypass, reverted immediately after, same technique as prior steps) at 390px width: the barn-less state correctly blocks goat registration and links to `/barns`; the empty-goats state, Add Goat dialog (all fields, conditional reproductive-state field, whitespace-only tag correctly rejected server-side), and the detail page (stage badge, tabs, coming-soon placeholders) all render correctly with zero console errors under a production build. One real bug was caught and fixed this way: two `<Button render={<Link .../>}>` usages (the "Go to Barns" and "Back to Goats" links) needed `nativeButton={false}`, since the Base UI `Button` primitive otherwise assumes its `render` target is a real `<button>` and logs an accessibility warning when it's actually an anchor. A `useSearchParams()`-based approach was also tried in `BarnFilter` first and produced a dev-server-only hydration warning (confirmed absent under `next build && next start`, so not a real bug) — it was still replaced with the `searchParams` page-prop approach the Next.js docs recommend for this exact case, which is simpler and doesn't require a `Suspense` boundary. Full authenticated CRUD and cross-account RLS isolation still need the owner's own logged-in test, same as barns.

- **Goat origin & breed composition** (`context/feature-specs/05a-goat-origin-and-breed.md`) — a follow-up to 05, additive only (a new migration, 05's own migration untouched). `supabase/migrations/20260826000002_goat_origin_breed.sql` adds `origin` (a `goat_origin` enum — `born_here` / `purchased` — to match how `sex`/`status` are already defined on `goats`, resolving that open question by inspection rather than by asking), `purchase_date`, `breed_secondary`, and `breed_primary_pct numeric(6,3)` with three check constraints (`goats_breed_pct_range`, `goats_breed_cross_consistency`, `goats_breed_distinct`) enforcing that a goat is either purebred (`breed_secondary is null`, `pct = 100`) or a two-breed cross (`pct` in `[50, 100)`, secondary distinct from primary). Existing rows backfill to purebred/born-here without violating the new constraints. `lib/goats/breeds.ts` has the portable `GOAT_BREEDS` list, `GRADING_PERCENTAGES` (50/75/87.5/93.75), `BreedComposition`, `isPurebred()`, and `formatBreed()` (e.g. `"87.5% Boer × 12.5% Somali (Galla)"`, trimming trailing zeros so `93.75` never renders as `93.750`). `app/(app)/goats/actions.ts` validates breed (required), the composition rules (secondary ≠ primary, percentage in range), origin, and purchase-date rules (only meaningful when `purchased`; not in the future; not before `date_of_birth`) — mirroring the DB constraints so the owner sees friendly errors, not raw Postgres ones. `components/goats/goat-form-dialog.tsx` gained: a breed `Select` (the five listed breeds + "Other…", which reveals a free-text input), a purebred/crossbred toggle that reveals a secondary-breed `Select` (same list + Other) and a percentage `Select` (the standard grading steps + a numeric "Custom…" entry) with a live read-only preview via `formatBreed()`, and an origin `Select` that conditionally reveals a purchase-date field only when "Purchased" is chosen. The list and detail pages now show the formatted breed and origin/purchase-date. Two real bugs were found and fixed during verification, both pre-existing patterns in this Base UI/shadcn setup rather than anything specific to 05a's own new code: (1) the breed and percentage `Select`s were displaying their raw internal sentinel values (e.g. the literal string `__other__`) in the closed trigger instead of a label — fixed by passing an `items` array to the `Select` root, which this Base UI version uses to resolve the trigger's displayed label independently of the submitted value; (2) the **existing** (05) Barn `Select` had the same root-cause bug, silently showing a barn's raw numeric id instead of its name once selected — never caught in 05 because no prior verification pass had actually selected a barn and looked at the closed trigger. Fixed the same way in both `goat-form-dialog.tsx` and `components/goats/barn-filter.tsx`. `npm run build` passes; the generated-types wiring was re-confirmed by temporarily selecting a nonexistent column and watching it fail, then reverting. Verified in a real browser (Playwright, same temporary auth-bypass-and-stub-data technique as 05, fully reverted after) at 390px width: the breed dropdown (all 5 breeds + Other), the purebred/crossbred toggle, the live composition preview (confirmed exact, unrounded, for both 75% and 87.5%), the Other-breed text override, the origin toggle's conditional purchase-date field, and server-side rejection of a same-primary-and-secondary breed all behaved correctly with zero console errors under a production build.

- **Goat form restructure & breed-cross input, and the dialog-trigger hydration fix** (`context/update-specs/002-goat-form-and-breed-cross.md` `UPD-002`, shipped together with `context/error-specs/001-goat-dialog-trigger-hydration.md` `ERR-001` — both touch `components/goats/goat-form-dialog.tsx`). The goat form is now origin-driven: a shadcn/base-ui `ToggleGroup` segmented control (Born on the farm / Purchased, added via the CLI) sits at the top and conditionally shows Purchase date and reshapes the breed input. Breed is now one unified `Select` for both origins (pure breeds + `Crossed…` + `Other…`) rather than `05a`'s separate purebred/crossbred toggle plus percentage picker — picking `Crossed…` reveals "First parent breed" / "Second parent breed" (must differ, inline error otherwise), computed via the new `crossOfPureBreeds()` in `lib/goats/breeds.ts` (always 50/50, live preview "= 50% X × 50% Y"). Owner confirmed up front (per `UPD-002`'s flagged open question) that Purchased should only **default** to purebred, not lock to it — `Crossed…` stays selectable there too. State hygiene: switching to Purchased while `Crossed…` was selected resets breed back to unselected rather than keeping a stale cross; switching to Born-on-farm simply stops rendering (and so never submits) purchase date. A `crossTouched` flag preserves an **existing** cross's real stored percentage (e.g. an older 87.5% grade from `05a`) in the live preview and submitted value until the owner actively changes a parent-breed pick, so re-saving an untouched cross can't silently rewrite it to 50%. No server-action or schema changes were needed — `05a`'s validation already covers everything the new UI submits. For `ERR-001`: `GoatFormDialog` and `BarnFormDialog` no longer accept a `trigger: ReactElement` prop from their Server Component callers; each now builds its own trigger `<Button>` internally from plain `triggerLabel`/`triggerIcon`/`triggerVariant`/`triggerSize` props, so no client element crosses the RSC boundary into `DialogTrigger`'s `render` slot. Applying the Preventive Rule turned up one more occurrence beyond the two named in the error spec — the goat detail page's Edit button (`app/(app)/goats/[id]/page.tsx`) had the identical pattern — fixed the same way; `delete-goat-dialog.tsx`/`delete-barn-dialog.tsx` were checked and already built their triggers internally, so they were never affected. `npm run build` passed and `tsc` was clean throughout. Verified with the same temporary auth-bypass-and-stub-data technique as prior sessions (env-gated, `app/(app)/layout.tsx` + `lib/supabase/middleware.ts` + stubbed data in the goats/barns list pages), driven by Playwright against a production build at 390px and 1280px: confirmed the segmented control, both conditional field sets, the cross-breed reveal/validation/preview, the origin-switch state hygiene, and — critically for `ERR-001` — zero console warnings or errors captured on the initial SSR paint of `/goats` and `/barns`. All temporary scaffolding was fully reverted and confirmed absent (`grep`) before a final clean `npm run build`. Full authenticated CRUD (an actual successful save) still needs the owner's own logged-in test, same outstanding item as every prior goat/barn session.

- **Family tree (`context/feature-specs/06-family-tree.md`)** — Phase 2's first feature, built in four verified increments. **6a — parents & pedigree:** `sire_id`/`dam_id`/`sire_name`/`dam_name` already existed on `goats` (05 migration), so no schema change; added `lib/goats/pedigree.ts` (pure, cycle-safe ancestry walk + `PEDIGREE_MAX_GENERATIONS = 4`, built for 09's inbreeding check to reuse), `components/goats/parent-picker.tsx` (in-system ⇄ external toggle, origin-aware default mode, sire→male / dam→female filter with a per-picker "show all" override), `components/goats/pedigree-view.tsx` (nested sire/dam tree, phone-scrollable), wired both pickers into `goat-form-dialog.tsx`, extended `createGoat`/`updateGoat` to accept + validate the parent fields (id XOR name, not-own-parent, referenced id must be one of the owner's goats), filled the Lineage tab on the detail page. **6b — multi-breed composition:** owner ran three migrations — `20260828000001_goat_breed_composition.sql` (child table + RLS + lossless backfill from the old two columns), then `20260828000003_drop_two_breed_columns.sql` (dropped `breed_secondary` / `breed_primary_pct` + their three check constraints, after the verify query returned no rows). `goats.breed` is kept as a denormalised primary-breed label. `lib/goats/breeds.ts` rewritten to a list model: `BreedComposition = { breed: string; pct: number }[]`; `formatBreed`/`isPurebred`/`crossOfPureBreeds` updated; added `normalizeComposition`, `validateComposition`, `primaryBreed`. Server actions take a `breed_composition` JSON field, validate it (app-level sum-to-100 check, no DB trigger), and replace the goat's `goat_breed_composition` rows (delete + insert). The form, list, detail, and pedigree read the composition via an embedded `goat_breed_composition` select. **6c — parent-based breed computation:** `composeFromParents(dam, sire)` in `lib/goats/breeds.ts` averages the two parents' compositions per breed and normalises to 100 (unit-checked incl. the 50/25/25 three-breed example). In the goat form, when a **born-here** goat has **both** parents set to in-system goats, a "Use parents' breed" ⇄ "Enter manually" toggle appears; it is never auto-applied and never silently overwrites a stored composition (a 3+ breed goat shows read-only until a new breed is picked). **6d — barn-move history:** owner ran `20260828000002_goat_barn_moves.sql`; added `moveGoatToBarn` (two-step update-then-insert, non-atomic — acceptable for a single-owner app), `components/goats/move-barn-dialog.tsx` (trigger built inside per `ERR-001`; excludes the current barn), `components/goats/barn-move-history.tsx`, wired into the detail page. `npm run build` + `tsc --noEmit` pass. Browser-verified with the usual temporary auth-bypass + stub-data technique (fully reverted, `grep`-confirmed, clean final build): all four increments render and behave correctly with **zero console warnings or errors** on first paint at 1280px and 390px, including parent-based computation producing `75% Boer × 25% Somali (Galla)` live in the dialog. Owner's authenticated save + cross-account RLS test on the two new tables is still outstanding, same as barns/goats. Open-question decisions are recorded in `06-family-tree.md` §16.

- **Goat form multi-step wizard** (`context/update-specs/003-goat-form-multistep-wizard.md` `UPD-003`, `done`) — the goat add/edit form is now a four-step wizard inside the same dialog: **Identity & Origin → Breed & Housing → Parents (skippable) → Notes & Review**, with an always-visible "Step X of 4" indicator + segmented progress bar. The stepper is a new **reusable, goat-agnostic pattern** in `components/forms/`: `use-wizard-steps.ts` (`useWizardSteps` hook — navigation state only, the consuming form owns all field values so steps never drop data; `allowJump` for edit mode), `step-indicator.tsx` (`<StepIndicator>`, token-styled), `wizard-nav.tsx` (`<WizardNav>` — generic Back/Next/Skip, submit button supplied by the consumer for the last step). `07` and `09` are expected to import these directly. In `goat-form-dialog.tsx`: one `<form>` wraps all four steps, each step a `<div>` toggled `flex`/`hidden` (never unmounted) so the single final submit still collects every field — **one DB write on Save**, calling the unchanged `createGoat`/`updateGoat`. `tag`/`date_of_birth`/`barn_id` moved to controlled state (keeping their `name` attrs) so per-step "Next" gating can read them; **no validation rule changed** — `step1Valid` (tag + non-future DOB) and `step2Valid` (resolved breed + barn) are exactly the server action's existing conditions, just checked per-step; the server action stays the authority. Step 3 is built directly as the Parents step from `06`'s `ParentPicker`s (reused unchanged); "Skip for now — add later" advances (and, in the create flow only, clears + remounts the pickers empty). 6c's "Use parents' breed" toggle stays with the Breed field on step 2 and appears reactively once both parents are in-system goats — no step reordering needed. `npm run build` + `tsc --noEmit` clean. Browser-verified with Playwright/Chromium at iPhone width (390×844, `isMobile`) via the usual env-gated auth-bypass + a throwaway `wizard-check` page (all reverted, `grep`-confirmed, clean final build): every step fits with **zero scroll**; "Next" gating, back/forward data persistence, the Skip affordance, the review summary, and edit-mode pre-fill + free step-jumping all behaved correctly with **zero console errors or warnings**. **The owner then tested the wizard directly in the running app, including at phone width, and confirmed it works (2026-08-28)** — covering the authenticated Save path; only cross-account RLS on a second real login is still outstanding, same as every prior goat session (schema and server action untouched). The paired doc change (`ui-context.md` "Forms — Length & Multi-Step Standard", `ai-workflow-rules.md` "Form Length Standard") was already in the repo when this was picked up. **Follow-up (not yet filed):** the owner has minor wizard refinements in mind for later — noted here and in `003-*.md` §13 so the intent isn't lost; a proper update spec will be written when they're defined.

## In Progress

- None.

## Next Up (build phases, in order)

1. **Core records:** family tree / pedigree (06 — done), health / vaccinations / deworming / medicine (07 — next), weight (08), breeding + inbreeding check (09, reuses `lib/goats/pedigree.ts`).
2. **Operations:** inventory and sales & purchases.
3. **Insight:** dashboard, graphs, analytics, calendar.
4. **Reference & output:** the "Doctor" health reference, to-dos / reminders, PDF reports.
5. **Follow-up (not phase-ordered):** goat photo upload to Supabase Storage, deferred from 05.

## Open Questions

- **Family-tree / breed-composition refinements to follow.** The owner tested `06` end to end (2026-08-28) and confirmed it works, but has some minor improvements in mind for the family-tree and breed-composition UX. Nothing is specced yet — an update spec (or a couple) will be filed when the owner is ready. Do not pre-empt it; this note just keeps it from being forgotten.
- **Logo colors:** the brand sand (`#D9A05B`) and terracotta (`#C56A3B`) accents are placeholders in the desert direction; replace with the farm logo's real hex once available.
- **Goat photo upload:** deferred from 05 to its own follow-up increment per the owner (2026-08-26). `photo_url` already exists on `goats` (nullable) and the spec's storage-bucket design (private `goat-photos` bucket, owner-scoped storage RLS, signed URLs) is ready to build from `05-goat-profiles.md` section 11 when picked up.
- **Legacy pre-spec tables collide with planned table names.** The Supabase project already has 8 tables predating the spec-driven rebuild (`goat_records`, `health_history`, `vaccinations`, `deworming`, `medicine_records`, `breeding_history`, `weight_history`, `sales_purchases`), each keyed on `goat_records.tag_number` rather than an owner-scoped id, and using the simple authenticated-only RLS from step 02 (noted in `04-barns.md` as "migrating them to owner scope is a later, separate job"). Three of their names (`vaccinations`, `medicine_records`, `sales_purchases`) exactly match table names specs **07** (health-records), **09** (breeding-and-inbreeding, via `breedings`/`breeding_history`), and **11** (sales-and-purchases) plan to create fresh with the new owner-scoped shape. Whoever builds those specs must resolve this collision first (rename/drop the legacy table, or reconcile into it) — confirmed with the owner on 2026-08-26 to log this now rather than address it during 05, since 05's own `goats` table doesn't collide with `goat_records`.

## Architecture Decisions

- **Stack:** Next.js 16 + TypeScript, Tailwind + shadcn/ui, Supabase (PostgreSQL + Auth + Storage), hosted on Vercel. All free tier.
- **Auth:** Supabase Auth, single owner, email / password; row-level security enforced with `auth.uid()`.
- **Data access:** Supabase client with generated TypeScript types (no Prisma).
- **Reusable backend:** Supabase is the single source of truth so future iOS / Android apps share it; keep shared logic (inbreeding check, stage derivation) as portable functions in `lib` that can move to a Supabase function later.
- **Barns are core:** every goat is assigned a barn at registration; `barn_id` is nullable in the database for edge cases. Grouped so a future Farm → Barn → Goat layer can be added without rework.
- **Goat stage** (Doe, Doeling, Buck, Buckling, Wether, Kid) is derived from sex, age, and reproductive state — not stored. Thresholds confirmed with the owner (2026-08-26): `kidMax: 6` months, `youngStockMax: 12` months, general-purpose rather than breed-specific; kept in one place (`lib/goats/stage.ts`) so they're easy to retune per breed later.
- **Goat identifier:** per the owner (2026-08-26), `tag` is the required identifier on a goat record and `name` is optional — the reverse of the original spec draft. Every later spec/UI referencing a goat's "name" should treat `tag` as the fallback display label when `name` is blank (see `goat.name ?? goat.tag` in the goats list/detail pages).
- **New-table id convention:** `barns` (04) set the real convention as `bigserial` integer ids with a single `"owner full access" for all` RLS policy, not the `uuid` + four-policy style shown in some spec drafts. `goats` (05) followed suit; later specs creating new tables should match this too unless there's a specific reason not to.
- **Goat breed & origin** (05a, confirmed with the owner 2026-08-26): breed is required in the goat form (the `breed` DB column stays nullable for imports); the standard grading-up percentage set (50 / 75 / 87.5 / 93.75 + custom) covers the owner's crossing practice as-is; purchase date is optional even when origin is Purchased; no seller/`purchased_from` column was added (revisit if/when module 11 wants to link a purchase record). `origin` is a Postgres enum (`goat_origin`: `born_here` / `purchased`), matching how `sex`/`status` are already defined on `goats` — this was resolved by inspecting the existing schema, not by asking, since the spec explicitly said to match the existing convention.
- **Base UI `Select` trigger display:** this shadcn/Base UI `Select` only shows a submitted value's real label in the closed trigger if the `Select` root is given an `items` array (`{ label, value }` pairs); without it, the trigger displays the raw `value` string verbatim. Any `Select` whose values aren't already human-readable on their own (sentinels like an "Other…" placeholder, or opaque ids like a barn's numeric `id`) needs `items` set, or it'll silently show internal/meaningless text to the owner. Selects whose values already read fine on their own (e.g. `sex`, `status` — lowercase but legible words) don't strictly need it, though it would be a worthwhile cosmetic pass later.
- **Dialog trigger convention (`ERR-001`, 2026-08-28):** a form dialog must never accept a pre-built trigger element (e.g. `trigger: ReactElement`) from a Server Component caller — passing a client `<Button>` element across the RSC boundary into base-ui's `DialogTrigger render={...}` resolves `data-slot` differently on SSR vs. the client and causes a hydration mismatch. Every dialog instead builds its own trigger internally from plain props (`triggerLabel`, `triggerIcon`, `triggerVariant`, `triggerSize`), as `GoatFormDialog` and `BarnFormDialog` now do. Any new add/edit dialog should follow this shape from the start rather than the old element-prop pattern.
- **Breed is a composition, stored in a child table** (`06`, 6b, 2026-08-28): `goat_breed_composition` (one row per breed component, `pct numeric(6,3)`), replacing `05a`'s two `goats` columns. Chosen over a JSON column for RLS consistency (one `"owner full access"` policy, like every other owned table) and easy querying. `goats.breed` is **kept** as a denormalised primary-breed label so list/table views don't need a join. The sum-to-100 rule is enforced app-side in `validateComposition` (`lib/goats/breeds.ts`), not a DB trigger — sufficient for v1. `BreedComposition` is `{ breed: string; pct: number }[]`; a purebred goat is one row at 100, a manual cross is two rows at 50/50, and parent averaging (`composeFromParents`) can yield three or more rows.
- **Pedigree / ancestry walk is pure `lib` logic** (`06`, 6a): `lib/goats/pedigree.ts` — no React, no Supabase, callers pass an in-memory `Map` of goats. Cycle-safe (a goat repeating on its own path becomes a leaf). `PEDIGREE_MAX_GENERATIONS = 4` in one place; **spec 09's inbreeding check must reuse `buildPedigree` / `collectAncestorIds` from here** rather than re-walking `sire_id`/`dam_id` itself (invariant 6).
- **Parent-based breed computation is offered, never auto-applied** (`06`, 6c): the goat form shows a "Use parents' breed" ⇄ "Enter manually" toggle only when a born-here goat has both parents linked to in-system goats; the manual input stays the default and an existing stored composition is never silently overwritten. General rule for this project: when a feature *could* auto-compute a value the owner may have entered by hand, make it an explicit choice.
- **Long forms are multi-step wizards, built on the shared `components/forms/` stepper** (`UPD-003`, 2026-08-28): the "Forms — Length & Multi-Step Standard" in `ui-context.md` is now a standing rule (any form past ~6–7 fields, or with a natural sub-topic, or an optional skippable section → wizard, checked at phone width). The reusable pieces are `components/forms/use-wizard-steps.ts` / `step-indicator.tsx` / `wizard-nav.tsx` — deliberately goat-agnostic. `07` (health records) and `09` (breeding) must compose their forms from these rather than re-inventing pagination. Key design point: the wizard is pure UI pagination — the consuming form holds one in-memory state object across all steps and does a single write on final submit; steps are hidden (`flex`/`hidden`), never unmounted, so nothing is lost navigating between them.
- **Two new-table migrations run atomically in the app as two steps, not a Postgres RPC** (`06`, 6b/6d, per spec §16): `moveGoatToBarn` (update `goats.barn_id` then insert `goat_barn_moves`) and `writeBreedComposition` (delete then re-insert `goat_breed_composition` rows) can half-apply on a mid-call crash. Accepted for a single-owner app; revisit with a `move_goat` / composition RPC if it ever matters.
- **Libraries:** Recharts (charts), react-big-calendar (calendar), @react-pdf/renderer (PDF reports).
- **Dropped from the original template:** Prisma, Clerk, Liveblocks / React Flow, Trigger.dev, Vercel Blob, AI generation, and the design canvas.
- **Deferred (not now):** multiple farms, breeding season planning, seasonal health analysis, cow tracking, and native mobile apps.

## Session Notes

- 2026-08-24: Context pack adapted from an AI-course template to this goat farm app. Next action is to scaffold the Next.js + Supabase project and begin Phase 1.
- 2026-08-24: Implemented the design system (`01-design-system.md`). Gotcha for future token work: naming a `@theme inline` color key the same as a reserved Tailwind v4 scale key (e.g. `base`, which the font-size scale uses for `text-base`) makes Tailwind merge the two utilities and silently drop the font-size rule. Fixed by defining `bg-base` as a scoped `@utility` instead of a generic `--color-*` theme key. Watch for this with any new token name that matches a Tailwind default scale key (`sm`, `md`, `lg`, `xl`, `2xl`, `full`, `none`, etc.).
- **Security:** rotate any previously exposed Supabase anon key. The anon key is safe in the browser only with row-level security enabled; the service-role key is server-only and never committed.
- 2026-08-25: Implemented auth and login (`02-auth-and-login.md`). `.env.local` was scaffolded with placeholder Supabase values (not committed — already gitignored via `.env*`); the owner still needs to create the owner account and run the RLS SQL from the Supabase dashboard (see the "Manual steps for the owner" list delivered in that session's reply). `middleware.ts` from the spec became `proxy.ts` because this Next.js version (16.3.2) deprecated the former.
- 2026-08-25: Implemented the app shell (`03-app-shell.md`). To verify the sidebar/drawer against real routes and real `usePathname()` values (rather than a parallel test page that wouldn't reflect the actual URLs), the session guard in `proxy.ts` and `app/(app)/layout.tsx` was temporarily bypassed, checked in a real browser at desktop and iPhone widths, then reverted immediately — confirmed back to matching the step-02 behavior afterward. Caught a real bug this way: the mobile drawer didn't close on nav-link tap (fixed in `components/app-sidebar.tsx`, see Completed). No Supabase/Vercel dashboard steps were needed for this feature.
- 2026-08-25: Implemented barns (`04-barns.md`), the CRUD template for every later module. Initially built with hand-written types (no `SUPABASE_ACCESS_TOKEN` available yet). The owner then added `SUPABASE_ACCESS_TOKEN` to `.env.local` in the same session; `npm run gen:types` was run with the token read out of that file and passed via a PowerShell environment variable that was never echoed, printed, or written into any command text, and cleared immediately after. Real generation confirmed the hand-written `barns` shape had been exactly right, and also revealed the owner had already run the `barns` table + RLS SQL themselves — `/barns` now reflects a real, existing (empty) table rather than a missing one. `gen:types` itself had to switch from a bare `supabase` call to `npx supabase`, since the CLI isn't installed globally in this environment. Full authenticated CRUD (add/edit/delete, RLS isolation across two accounts) still needs the owner's own logged-in test, since only they have the credentials.
- 2026-08-26: Implemented goat profiles core (`05-goat-profiles.md`), deferring the photo-upload task. Before writing schema, discovered the Supabase project already has 8 tables predating this spec workflow (`goat_records` and friends) — confirmed with the owner that this is a legacy leftover to log as an open question for specs 07/09/11, not something to fix now (see Open Questions). Asked the owner up front about the spec's four flagged open questions: stage thresholds (kept the spec's own placeholder, 6/12 months), name-vs-tag (owner wants `tag` required and `name` optional — the reverse of the spec draft), DOB required (kept as spec default), and photo bucket privacy (private bucket + signed URLs, kept as spec default, though the task itself was deferred). Wrote `supabase/migrations/20260826000001_goats.sql` and asked the owner to run it in the Supabase SQL editor (same manual step as barns, since this agent has no direct DB write access — only the type-gen access token); the owner's `SUPABASE_ACCESS_TOKEN` had gone blank in `.env.local` since the barns session and needed refreshing before `npm run gen:types` would work again. Reconciled the migration against the real `barns` convention (`bigserial` ids, single RLS policy) rather than the spec's literal `uuid`/four-policy sample. Built `lib/goats/stage.ts`, the CRUD server actions, list page with barn filter, add/edit dialog, and detail page with placeholder history tabs. Added shadcn `select`, `badge`, and `table` components via the CLI. Verified in a real browser: running `npm run dev` in the background was blocked by the auto-mode classifier and required the owner's explicit approval before proceeding; Playwright/Chromium (fetched fresh via `npx`, not pre-installed in this environment) confirmed the barn-less state, empty-goats state, the Add Goat dialog's conditional reproductive-state field, and server-side whitespace-tag validation, all with zero console errors under a production build (`next build && next start` — a dev-mode-only hydration warning first appeared in `next dev` but was confirmed to be a Turbopack dev-server artifact, not a real bug, by reproducing its absence in production). One real bug was caught and fixed: `<Button render={<Link/>}>` needs `nativeButton={false}` or Base UI logs an accessibility warning, since it otherwise assumes a real `<button>`. All temporary verification hacks (auth bypass in `proxy.ts`/`app/(app)/layout.tsx`, stubbed barn/goat data in the two page components) were reverted before finishing, confirmed via a clean `npm run build`. Full authenticated CRUD and cross-account RLS isolation still need the owner's own logged-in test, same as barns.
- 2026-08-26: Implemented goat origin & breed composition (`05a-goat-origin-and-breed.md`), an additive follow-up to 05. Asked the owner all four flagged open questions up front (breed required, grading percentages, purchase-date required, seller column) and kept the spec's own defaults for all of them; resolved the fifth (origin's enum-vs-text style) by inspection, per the spec's own instruction to match `sex`/`status`, rather than asking. Wrote `supabase/migrations/20260826000002_goat_origin_breed.sql` and asked the owner to run it in the SQL editor (same manual step as 05/04); regenerated types once confirmed. Built `lib/goats/breeds.ts`, extended `app/(app)/goats/actions.ts` with composition/origin/purchase-date validation, and reworked `components/goats/goat-form-dialog.tsx` with the new breed/composition/origin fields. Verified in a real browser (same temporary auth-bypass-and-stub-data technique as 05, fully reverted after): found and fixed two real bugs along the way — the breed/percentage `Select`s leaking their raw sentinel values (e.g. literally showing `__other__`) into the closed trigger, and an **existing bug from 05** where the Barn `Select` showed a barn's raw numeric id instead of its name once selected (only surfaced now because this was the first verification pass to actually pick a barn and look at the closed trigger) — both fixed by passing an `items` array to the affected `Select` roots, which is how this project's Base UI version resolves a trigger's display label independently of its submitted value. See Architecture Decisions for the general lesson. `npm run build` passes; zero console errors under a production build.
- 2026-08-28 (cont.): Owner ran `20260828000001_goat_breed_composition.sql` and `20260828000002_goat_barn_moves.sql` — both succeeded. `npm run gen:types` then failed with `Unauthorized` (the `SUPABASE_ACCESS_TOKEN` in `.env.local` is present but expired) and its error output truncated `types/database.types.ts` to 150 bytes via the `>` redirect. **Gotcha for future type-gen:** the `gen:types` script pipes stdout straight to the file, so a failed CLI call overwrites the real types with an error blob — check the file size / re-run after any `gen:types` failure. Restored `types/database.types.ts` by hand from the last known-good content and hand-added the `goat_barn_moves` and `goat_breed_composition` table types to match the two migrations (same stand-in approach used for `barns` in the 04 session, to be re-confirmed against a real `gen:types` once the token is refreshed). `tsc` passes on the restored file. Still needed before 6b/6c/6d can be built and verified: owner refreshes `SUPABASE_ACCESS_TOKEN`, runs a one-line verify query, then runs `20260828000003_drop_two_breed_columns.sql`.
- 2026-08-28: Started feature **06 — family tree**. Confirmed the `UPD-002`/`ERR-001` documentation sync from the prior session was already complete in the repo (both specs' status, approval, Sections 11–13 / Resolution Notes, the roadmap 05a note, and this tracker's Completed + Architecture Decisions + Session Notes entries were all already present) — no further doc changes were needed there. Resolved 06's Section 15 open questions by inspection/best-judgement rather than asking (recorded in `06-family-tree.md` Section 16): child table for breed composition, keep `goats.breed` as a denormalised label, offer "use parents' breed" as an explicit choice, default sire→male/dam→female picker filter with a per-picker "show all" override, `PEDIGREE_MAX_GENERATIONS = 4`, two-step (non-RPC) barn move. Built **6a** (parents & pedigree): the `sire_id`/`dam_id`/`sire_name`/`dam_name` columns already existed from the 05 migration so no schema change was needed — added `lib/goats/pedigree.ts` (pure, cycle-safe ancestry walk + `PEDIGREE_MAX_GENERATIONS`, built for 09 to reuse), `components/goats/parent-picker.tsx` (in-system-or-external toggle, origin-aware default mode, sex filter with override), `components/goats/pedigree-view.tsx` (nested sire/dam tree, phone-scrollable), wired both parent pickers into `goat-form-dialog.tsx`, extended `createGoat`/`updateGoat` to accept and validate the parent fields (id XOR name, not-own-parent, referenced id must be one of the owner's goats), and filled the Lineage tab on the goat detail page. `npm run build` and `tsc --noEmit` both pass clean. Browser verification of 6a is still outstanding (the dev server on :3000 was running unauthenticated; the established auth-bypass-and-stub technique wasn't run this session). **6b and 6d each need the owner to run a new migration in the Supabase SQL editor before their code can be built** — SQL delivered in the session reply.
- 2026-08-28 (06 complete): After the owner refreshed `SUPABASE_ACCESS_TOKEN`, ran the breed-composition verify query (no rows) and `20260828000003_drop_two_breed_columns.sql`, regenerated `types/database.types.ts` for real (confirmed the hand-added `goat_barn_moves` / `goat_breed_composition` shapes matched exactly, and `breed_secondary` / `breed_primary_pct` are gone from `goats`). Then finished **06**: built **6d** (barn-move history — `moveGoatToBarn`, `move-barn-dialog.tsx`, `barn-move-history.tsx`), **6b** (rewrote `lib/goats/breeds.ts` to the `{breed,pct}[]` list model; `createGoat`/`updateGoat` now take a `breed_composition` JSON field and replace `goat_breed_composition` rows; form/list/detail/pedigree read the embedded composition), and **6c** (`composeFromParents` + the "Use parents' breed" toggle in the born-here form path). Unit-checked the breed math with `tsx` (all spec examples incl. the 50/25/25 three-breed case + odd-thirds → sum 100). Browser-verified each increment with the usual env-gated auth-bypass + stub-data technique against `next start` on port 3100 (kept off the owner's :3000 dev server), driven by Playwright at 1280px and 390px: 6a pedigree (all node kinds, cycle-safe), 6d move dialog + history, 6b `formatBreed` on the list, 6c computing `75% Boer × 25% Somali (Galla)` live in the dialog — **zero console warnings or errors** on first paint throughout. All scaffolding reverted and `grep`-confirmed absent; final `npm run build` + `tsc --noEmit` clean. Two mishaps to note: (1) the first `gen:types` run (before the token was refreshed) failed `Unauthorized` and its error text truncated `types/database.types.ts` via the `>` redirect — recovered by hand-restoring; **always check the file after a `gen:types` failure**. (2) While checking port state at the end, a `foreach ($p in 3000,3100){ Stop-Process … }` loop **accidentally killed the owner's `next dev` server on :3000** — no data or code impact, but the owner needs to restart it with `npm run dev`. 06 roadmap row + all four sub-feature rows set to `done`, `◀ next` moved to 07, `06-family-tree.md` status `done` with §16 decisions and a verification summary.
- 2026-08-28: Implemented `UPD-003` (goat form multi-step wizard). Picked up after confirming with the owner that the repo was already ahead of the task framing — the paired doc sync (`ui-context.md` / `ai-workflow-rules.md` form standard) was already present, and feature 06 was already fully `done`, so "pause/resume 6b/6c/6d" was moot; only UPD-003 itself remained. Built the reusable stepper in `components/forms/` (`use-wizard-steps.ts`, `step-indicator.tsx`, `wizard-nav.tsx` — no goat logic) and restructured `goat-form-dialog.tsx` into the 4-step wizard (Identity & Origin / Breed & Housing / Parents [skippable, built directly as step 3 from 06's `ParentPicker`] / Notes & Review). All steps stay mounted (`flex`/`hidden`) under one `<form>` so the single final submit is unchanged; moved `tag`/`dob`/`barn_id` to controlled state for per-step "Next" gating without changing any validation rule. `npm run build`/`npm run build` (with temp scaffolding) needed owner approval again because the auto-mode classifier blocks build/start once the env-gated `WIZARD_VERIFY` auth-bypass + throwaway `wizard-check` page were added — approved, verified with Playwright at 390×844 (every step zero-scroll, gating + persistence + skip + edit pre-fill all correct, zero console errors), then all scaffolding reverted and `grep`-confirmed absent before a clean final `npm run build` + `tsc --noEmit`. Killed only the port-3210 verification server by resolving its PID via `Get-NetTCPConnection -LocalPort 3210` (not a broad port loop — avoiding the earlier mishap that killed the owner's :3000 dev server). Authenticated Save is still the owner's outstanding test. UPD-003 set `done` with §§11–13 filled; roadmap 05a follow-ups prose + this tracker updated in the same step.
- 2026-08-28 (UPD-003 signed off): The owner tested the goat-form multi-step wizard directly in the running app, including at phone width, and confirmed it works — this covers the authenticated Save path agent testing couldn't reach; only cross-account RLS on a second real login stays outstanding, same as every prior goat session. `003-goat-form-multistep-wizard.md` §12 now records the owner acceptance and §13 carries a "minor wizard refinements may follow, not yet filed" follow-up note; the roadmap 05a follow-ups prose already reflects `UPD-003` `done`. Per the owner, those refinements are noted (spec §13 + the Completed entry above) but **no new update spec is filed yet** — one will be written when they're defined.
- 2026-08-28: Implemented `UPD-002` (goat form restructure & breed-cross input) together with `ERR-001` (goat dialog trigger hydration mismatch), shipped as one unit since both touch `components/goats/goat-form-dialog.tsx`. Asked the owner the one flagged open question up front (`UPD-002` §5/§8: should Purchased lock to purebred, or just default to it) — owner chose default-only, matching the spec's literal text, so `Crossed…` stays selectable on both origin paths via one unified breed `Select`. Added the shadcn/base-ui `ToggleGroup` component via the CLI for the origin segmented control (not previously in `components/ui/`). Added `crossOfPureBreeds()` to `lib/goats/breeds.ts`; reworked `components/goats/goat-form-dialog.tsx` around the origin toggle and the unified breed dropdown, replacing `05a`'s purebred/crossbred toggle + percentage picker entirely; no server-action or schema changes were needed. For `ERR-001`, removed the `trigger: ReactElement` prop pattern from `GoatFormDialog` and `BarnFormDialog` (each now builds its own trigger button internally from plain label/variant props) and fixed all of their call sites in `app/(app)/goats/page.tsx`, `app/(app)/goats/[id]/page.tsx`, and `app/(app)/barns/page.tsx` — applying the Preventive Rule caught an extra occurrence on the goat detail page's Edit button that the error spec hadn't named. Mid-session, `npm run build`/`npm run dev` got blocked by the auto-mode classifier specifically after temporary `TEMP_VERIFY_BYPASS`-gated auth-bypass scaffolding was added for browser verification (the same kind of block hit in the 05 session); asked the owner to approve, which they did, and verification proceeded normally afterward. Verified with Playwright against a production build (390px and 1280px): confirmed the segmented control, the conditional field sets, the cross-breed reveal/must-differ validation/live preview, the origin-switch state-hygiene reset, that editing a stubbed pre-existing 87.5% cross goat preserves its real percentage until a parent-breed select is touched, and — for `ERR-001` — zero console warnings or errors on the initial SSR paint of `/goats` and `/barns`. All temporary scaffolding was fully reverted and confirmed absent via `grep`, followed by a final clean `npm run build`. Full authenticated CRUD (an actual successful save) still needs the owner's own logged-in test, same outstanding item as every prior goat/barn session.
- 2026-08-28 (06 signed off): The owner tested all four family-tree sub-features (6a parents & pedigree, 6b breed-composition migration, 6c parent-based breed auto-computation, 6d barn-move history) directly in the running app and confirmed they work. Closed out the docs in one pass: `feature-specs-roadmap.md` — `06` is `done` in both the "At a glance" table and its section, all four sub-feature rows `done`, `◀ next` on `07`, and the `***` footnote records the owner's real-app confirmation; `06-family-tree.md` — status `done`, §16 decision table updated to "shipped as decided" with the app-level sum check and the shared two-step write pattern noted, plus an owner sign-off paragraph; this file — `06` moved from In Progress (now "None") to Completed, Current Phase / Current Goal advanced to `07` (health records, which must first resolve the legacy `vaccinations` / `medicine_records` table-name collision), Architecture Decisions gained the breed-composition / pedigree-walk / offered-not-auto / two-step-write entries. Per the owner, **minor family-tree / breed-composition UX refinements may follow** — logged under Open Questions above, no update spec filed yet. (Separately, between sessions `components/goats/goat-form-dialog.tsx` was reworked into a multi-step wizard under `UPD-003`, already `done` and documented in Completed — 06's `6a`/`6c` pieces, the `ParentPicker`s and the "Use parents' breed" toggle, carry into it unchanged; not touched here.) Standing item unchanged: cross-account RLS on `goat_breed_composition` / `goat_barn_moves` needs a second real login.

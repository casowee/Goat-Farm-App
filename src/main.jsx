import React from "react";
import ReactDOM from "react-dom/client";
import GoatFarmApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GoatFarmApp />
  </React.StrictMode>
);
